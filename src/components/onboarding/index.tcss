.onboarding {
  background-color: var(--primary-color);
}

.onboarding-img {
  width: 100px;
  height: 100px;
  margin-bottom: 30px;
}

.onboarding-text {
  font-size: 16px;
  color: #fff;
  font-style: italic;
  padding: 0 10px;
}

.onboarding-text text {
  color: var(--second-color);
}
