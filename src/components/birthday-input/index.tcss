.birthday-selector {
  position: relative;
}

.birthday-selector::after {
  content: "";
  position: absolute;
  top: 37px;
  left: 0;
  width: 80vw;
  height: 40px;
  background-color: transparent;
  border-top-left-radius: 20px;
  border-bottom-left-radius: 20px;
}
