<view class="header flex items-center justify-start w-full">
  <block tiki:if="{{isShowBtn}}">
    <button class="header-btn border-none flex justify-center items-center" onTap="onTap">
      <icon type="arrow_left" size="20" color="#fff" />
    </button>
  </block>
  <text class="title">{{title}}</text>
</view>