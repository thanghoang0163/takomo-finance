.header {
  background-color: var(--primary-color);
  padding-top: 5vh;
  margin-bottom: 20px;
}

.header-btn {
  top: 33px;
  left: 0;
  border: none;
}

.header-btn:hover {
  background-color: transparent;
}

.header-img {
  width: 50px;
  height: 30px;
}
