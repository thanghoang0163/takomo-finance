.header {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100vw;
  height: 60px;
  background-color: var(--primary-color);
  margin-top: 4vh;
}

.header-img {
  width: 50px;
  height: 30px;
}
