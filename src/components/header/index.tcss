.header {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  height: 60px;
  background-color: var(--primary-color);
  padding-top: 5vh;
}

.header-img {
  width: 50px;
  height: 30px;
}
